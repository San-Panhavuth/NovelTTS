{
  "name": "noveltts",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-voice audiobook generator from EPUB uploads",
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev:frontend": "dotenv -e .env -- pnpm -F frontend dev",
    "dev:worker": "dotenv -e .env -- pnpm -F worker dev",
    "dev:backend": "dotenv -e .env -- uv run --project backend uvicorn --app-dir backend app.main:app --reload --port 8010",
    "lint": "pnpm -r --parallel lint",
    "typecheck": "pnpm -r --parallel typecheck",
    "build": "pnpm -r build",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\" --ignore-path .gitignore",
    "stack:up": "docker compose up -d",
    "stack:down": "docker compose down",
    "stack:logs": "docker compose logs -f"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "prettier": "^3.3.3"
  }
}
